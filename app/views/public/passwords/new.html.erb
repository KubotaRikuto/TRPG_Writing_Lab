<h2>パスワードを再設定</h2>

<%= form_for(resource, as: resource_name, url: member_password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "public/shared/error_messages", resource: resource %>
  <p>アカウントに登録したメールアドレスを入力してください。</p>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "パスワード再設定用のメールを送信" %>
  </div>
<% end %>

<%= render "public/shared/links" %>
